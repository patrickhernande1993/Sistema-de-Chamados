
// Serviço de IA removido.
// Mantendo a exportação da função como nula para evitar quebra de importações legadas temporariamente.

export const analyzeBillWithGemini = async (
  title: string,
  amount: number,
  category: string
) => {
  return null;
};
